@typeparam T
@foreach(var control in Value)
{
	@control
}
@code {
	[Parameter]
	public ICollection<T> Value{ get; set; }
	[Parameter]
	public EventCallback<ICollection<T>> ValueChanged{ get; set; }
}
